<view class="bar">
  <view
    wx:for="{{list}}"
    wx:key="pagePath"
    class="item {{active === index ? 'item--active' : ''}}"
    data-idx="{{index}}"
    bindtap="onTap"
  >
    <view class="dot"></view>
    <view class="label">{{item.text}}</view>
  </view>
</view>
